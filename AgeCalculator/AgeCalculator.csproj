<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net5.0</TargetFramework>
    <Authors>Arman Ghazanchyan</Authors>
    <Company>AG</Company>
    <Description>AgeCalculator can be used to calculate the age between two dates and output in years, months, and days format.</Description>
    <Product>Age Calculator</Product>
    <RepositoryUrl>https://github.com/arman-g/AgeCalculator</RepositoryUrl>
    <RepositoryType>Git</RepositoryType>
    <PackageTags>age calculator</PackageTags>
    <Version>1.0.1</Version>
    <PackageReleaseNotes>For a sample of the usage see the REDME file under the repository.</PackageReleaseNotes>
    <PackageProjectUrl>https://github.com/arman-g/AgeCalculator</PackageProjectUrl>
    <ApplicationIcon />
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DocumentationFile>AgeCalculator.xml</DocumentationFile>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <DocumentationFile>AgeCalculator.xml</DocumentationFile>
  </PropertyGroup>

</Project>
